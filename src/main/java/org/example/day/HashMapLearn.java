package org.example.day;

import java.util.HashMap;

public class HashMapLearn {


    /**
     * 第一步：先抓「核心主线」（记 3 个核心问题，覆盖 80% 考点）
     * 所有考点本质都是围绕这 3 个问题展开的，先把主线拎清，细节自然能推导出来：
     * 主线 1：HashMap 是「怎么存数据」的？
     * → 逻辑链：计算 hash 值 → 定位数组索引 → 没冲突直接存 → 有冲突先链表→链表太长转红黑树
     * 关键推导：
     * 为什么容量是 2 的幂？→ 为了用 hash & (length-1) 替代取模（效率高），且让索引分布均匀；
     * 为什么用红黑树？→ 链表查询 O (n) 太慢，红黑树 O (logn)；
     * 为什么链表转红黑树阈值是 8？→ 概率极低（泊松分布），极端情况才优化，避免频繁转换耗性能。
     * 主线 2：HashMap 「满了怎么办」？
     * → 逻辑链：元素到阈值 → 扩容（容量 ×2）→ 重新定位元素 → 迁移数据
     * 关键推导：
     * 阈值怎么算？→ 容量 × 加载因子（默认 0.75）；
     * 为什么加载因子是 0.75？→ 平衡「空间」和「冲突概率」（太高冲突多，太低浪费空间）；
     * JDK1.8 扩容更快？→ 不用重新算 hash，只看哈希值某一位是 0/1，要么留原索引，要么去「原索引 + 原容量」。
     * 主线 3：HashMap 「多线程用会怎么样」？
     * → 逻辑链：非线程安全 → 问题（JDK1.7 死循环、JDK1.8 数据丢失）→ 替代方案（ConcurrentHashMap 最优）
     * 关键推导：
     * 为什么不安全？→ 扩容时链表循环（1.7）、put 覆盖数据；
     * 替代方案怎么选？→ Hashtable / 同步包装类（锁整个表，慢），ConcurrentHashMap（分段锁 / CAS，快）。
     * 第二步：用「口诀 / 对比」记关键细节（不用记大段文字）
     * 把零散的数字、差异点做成「口诀 / 对比表」，一眼记住：
     * 1. 关键数字口诀（记 5 个核心数字就行）
     * 16（默认初始容量）、0.75（加载因子）、8（链表转红黑树）、6（红黑树转链表）、64（转红黑树的数组最小容量）
     * → 口诀：16 起步，75 分扩容，8 转树、6 转回，数组够 64 才转树。
     * 2. JDK1.7 vs 1.8 对比（记 3 个核心差异）
     * 表格
     * 维度	记结论（不用记细节）
     * 结构	1.7 只有链表，1.8 加了红黑树
     * 扩容	1.7 重新算 hash，1.8 直接判断位置
     * 插入	1.7 头插（易循环），1.8 尾插（安全）
     * 3. 高频对比题（记「核心差异」就行，不用记全）
     * HashMap vs Hashtable：
     * → 核心 3 点：线程安全（Hashtable 安全）、空值（HashMap 允许 null）、效率（HashMap 高）；
     * 自定义对象做 key：
     * → 核心结论：必须重写 hashCode+equals（hashCode 保证定位对，equals 保证判断对）；
     * → 为什么用不可变类（String/Integer）？→ 改了属性 hashCode 变了，找不到原来的值了。
     * 第三步：用「场景推导」代替死记（面试时能说清逻辑就行）
     * 面试时面试官不是要你背标准答案，而是要你说清「为什么」，比如：
     * 问：为什么加载因子是 0.75？
     * → 不用背「统计学最优值」，可以说：「加载因子太高的话，数组满了容易冲突；太低的话，空间浪费多。0.75 是平衡空间和冲突的折中值」；
     * 问：HashMap 为什么非线程安全？
     * → 不用背「死循环」细节，能说：「多线程扩容时 1.7 会出循环链表导致死循环，1.8 修复了但还是会数据丢失，所以不能在多线程用，替代的话用 ConcurrentHashMap」。
     * 总结
     * 记 HashMap 考点的核心技巧：
     * 抓 3 条主线：怎么存 → 满了怎么办 → 多线程用怎么样（覆盖 80% 考点）；
     * 记关键数字 / 对比：用口诀 / 简表记 5 个数字、3 个版本差异，不用记大段文字；
     * 用逻辑推导：面试时说清「为什么」比背「是什么」更重要，比如从「效率 / 空间」角度推导设计原因。
     */
    public void testHashMapLearn() {
        HashMap<Integer, String> map = new HashMap<Integer, String>();
        /**
         * https://blog.csdn.net/wuhuayangs/article/details/122470954?ops_request_misc=elastic_search_misc&request_id=dd4a5f30cc53470c044cd945ea9a55ac&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~top_positive~default-1-122470954-null-null.142^v102^pc_search_result_base1&utm_term=hashmap%E9%9D%A2%E8%AF%95%E9%A2%98&spm=1018.2226.3001.4187
         */
    }
}
